<?php

use Krystal\Form\Element;
use Krystal\Widget\Pagination\PaginationWidget;

?>

<div class="row">
    <div class="col-3">
        <form>
            <p><?php $this->show('Age filter'); ?></p>

            <div class="row">
                <div class="col-6">
                    <?= Element::number('age[start]', $age['start'], ['class' => 'form-control', 'min' => 0, 'max' => 100]); ?>
                </div>

                <div class="col-6">
                    <?= Element::number('age[end]', $age['end'], ['class' => 'form-control', 'min' => 0, 'max' => 100]); ?>
                </div>
            </div>

            <div class="row mt-4">
                <div class="col-12">
                    <p><?= Element::submit($this->translate('Search'), ['class' => 'btn btn-primary']); ?></p>
                </div>
            </div>
        </form>
    </div>

    <div class="col-9">
        <?php if (!empty($users)): ?>
        <div class="row">
            
            <?php foreach($users as $user): ?>
            <div class="col-4">
                <a href="<?= $this->url('User:Profile@indexAction', ['id' => $user['id']]); ?>"><img class="img-fluid" src="<?= $user['avatar']; ?>" /></a>

                <p><?= $user['name']; ?></p>
            </div>
            <?php endforeach; ?>
        </div>

        <?= $this->widget(new PaginationWidget($paginator)); ?>
        
        <?php else: ?>

        <p><?php $this->show('Nothing can be found'); ?></p>

        <?php endif; ?>
    </div>
</div>

